[package]
name = "defang"
version = "0.1.2"
edition = "2024"
description = "Defang and refang URLs, domains, IP addresses, and email addresses"
license = "MIT"

[[bin]]
name = "defang"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
